# DEVAGO Ceiling - заметки репозитория

Сайт-одностраничник потолочной студии DEVAGO (Ташкент). Клиент приходит через Икрома,
маркетингом занимается Артур. Стек: один `index.html` со встроенным CSS и JS, папка
`assets/` (изображения), папка `examples/` (эталонные макеты), `review/` (скриншоты
проверок). Ни сборки, ни зависимостей, ни фреймворков: файл открывается как есть.

Этот файл - память проекта. Всё, что ниже, раньше жило только в переписке рабочего чата.

---

## 1. Где остановились (состояние на 2026-08-04)

**БОЕВАЯ ВЕРСИЯ = v4 КАНОН, эта ветка `preview-mobile`.** Решение Тахира 03.08 после
того, как в один день по DEVAGO работали два окна и записали противоположные решения.
Рестайл REBEL v5.1 в бой НЕ идёт. Обоснование решения: Икром новичок, показывать надо
то, что он сам утверждал, иначе получаем круг правок «верните как было».

⚠️ **В ветке `redesign-rebel` (коммит `bb4af71`, уже в origin) записано обратное:
«боевая версия = v5.1».** Это ОТМЕНЕНО. Не верить той записи, верить этой.

**Владелец репозитория: чат GM-DEVAGO.** Чат вёрстки по DEVAGO не заходит (решение
Тахира 04.08 после столкновения двух сессий на одной рабочей копии).

Следующий шаг по порядку:
1. Забрать у Икрома свап-точки (см. раздел 4): телефон, Telegram, адресат заявок,
   подтверждение отзывов, вектор логотипа. У Артура коды Метрики и пикселя.
2. Перенести секцию фотопечати из `redesign-rebel` (услугу Икром подтвердил).
   Отзывы переносить НЕ надо, в каноне они уже живут внутри карточек проектов.
3. Только после явной команды Тахира: `preview-mobile` → `main`, это боевой деплой.

### Состояние веток

| Ветка | Что в ней | Прод |
|---|---|---|
| `main` | `aca6a4c` (24.07). Ранний макет по мокапу. Нет формы заявки, нет фотопечати, нет аналитики | ДА, это то, что открывается на devago-ceiling-site.vercel.app и что видят Икром и Артур |
| `preview-mobile` | **БОЕВАЯ.** Сборка v4.x: канон 1-в-1, аналитика, JSON-LD, две кнопки CTA, модалка формы, с 04.08 типографика Prata + Golos Text | нет, но именно она уедет в `main` |
| `redesign-rebel` | Рестайл v5.1 под референс REBEL. В бой НЕ идёт. Донор секции фотопечати | нет |

**Инвариант, который нельзя нарушать:** `main` = прод. Пока Тахир не сказал «деплой»,
в `main` ничего не мержим. Икром и Артур не должны видеть стройку.

**Инвариант, который нельзя нарушать:** `main` = прод. Пока Тахир не сказал «деплой»,
в `main` ничего не мержим. Икром и Артур не должны видеть стройку.

---

## 2. Что готово и проверено

- **Канон соблюдён 1-в-1.** Эталон - `examples/4.jpg` (десктоп) и `examples/5.jpg`
  (мобильный). Файлы `1-3.jpg` НЕ используются, это чужие варианты.
- **Тексты дословно с эталонных экранов**, ничего не переписано «своими словами».
- **Две языковые версии** RU/UZ, переключатель, перевод в том же регистре, meta
  description меняется вместе с языком.
- **Аналитика** за плейсхолдерами (`YM_PLACEHOLDER`, `PIXEL_PLACEHOLDER`): пока ключей
  нет, счётчик молчит и не ломает страницу. События: `hero_form_click`, `hero_tg_click`,
  `final_form_click`, `final_tg_click`, `form_submit` (= факт лида), `phone_click`.
- **Форма заявки** в модальном окне: Имя, Телефон (обязательный, маска +998), Комментарий.
- **SEO/шеринг**: JSON-LD `HomeAndConstructionBusiness`, og:image абсолютным URL,
  canonical, favicon и apple-touch.
- **Доступность 100** по Lighthouse на сборке v4, производительность 95, LCP 1.9 c.
- **Мобильная вёрстка проверена** на 390 и 360 px в обоих языках, горизонтального
  переполнения нет.
- **v5.1 дополнительно**: блок фотопечати (8 плиток, подписи дословные, RU+UZ), новый
  блок отзывов, карточки проектов и решений с фотографиями, типографика Manrope.

⚠️ Замер Lighthouse для v5.1 (рестайл) в текущих записях не подтверждён: цифры выше
относятся к v4. Перед сдачей рестайла прогнать заново, это 10 минут.

---

## 3. Принятые решения и почему именно так

- **Логотип обтравлен из `examples/logo.jpg`** через порог по яркости. Грязный край
  выглядит хуже родного фона, поэтому на светлых поверхностях логотип не используется.
- **Никаких длинных тире.** В текстах только запятые или короткое «-». Правило Тахира.
- **Никаких эмодзи** в интерфейсе. Иконки - только инлайновый SVG в цветах дизайна.
- **Две кнопки в hero и в финале** (решение группы Тахир + Артур + Икром): залитая
  золотом «Оставить заявку» открывает форму, контурная «Связаться в Telegram» ведёт
  в мессенджер напрямую. Одна кнопка теряла часть аудитории.
- **Ошибку отправки формы нельзя глотать молча.** Если запрос не прошёл, показываем
  честное «Не получилось отправить, напишите нам в Telegram» с живой ссылкой. Лид
  дороже, чем красивая заглушка.
- **Токен бота в клиентский код не кладём** ни при каких обстоятельствах. Транспорт
  формы = serverless-функция или бот на стороне сервера, согласовывается с Тахиром и CTO.
- **Липкая мобильная кнопка написана, но выключена** (`ENABLE_STICKY_CTA=false`):
  включать по решению, не по умолчанию.
- **Фотопечать подтверждена Икромом**, поэтому `ENABLE_PHOTOPRINT=true` и пункт в меню.
  Отдельной карточкой в блок решений её НЕ добавляли, это было отдельное указание.
- **Шапка на мобильном не липкая и без размытия**: в браузере Telegram липкая шапка
  с блюром «ползёт».
- **Типографика боевой версии (пара A, 04.08)**: заголовки `h1/h2/h3` в сериф **Prata**,
  весь интерфейс и текст в **Golos Text** (400/500/600/700), вордмарк DEVAGO остаётся
  в **Cormorant** через отдельную переменную `--wordmark`. Manrope снят полностью.
  Причина замены: клиент просил «премиальнее, но главное читабельность», а Тахир
  назвал прежний шрифт «чатгптшиным», то есть дефолтным интерфейсным гротеском.
  Найдено при разборе: на проде заголовки серифные (Cormorant), а обе наши сборки
  перевели их на Manrope, поэтому мы сами уехали в ту сторону, на которую жалуются.
  - **У Prata единственный вес 400.** Любой вес выше даст синтетический жирный и
    испортит рисунок. В v4 это безопасно: ни один заголовок не просит больше 400.
  - **Заголовок модалки намеренно остаётся на гротеске** (`.modal h3` со своим
    `font:400 ... var(--sans)`), это не недосмотр.
  - **Два веса 300 у `.combo-t-mob` и `.combo-t-desk`** («Идеальная комбинация»)
    оставлены как есть: в наборе Golos 300 нет, отрисуется 400, синтетики не будет.
    Заголовок задуман светлым, и эта лёгкость теряется. Вопрос открыт у CDO,
    объявление в коде сохранено намеренно, чтобы замысел не забылся.
  - **Цена решения замерена, а не оценена**: шрифты потяжелели с 226.5 КБ до 255.7 КБ,
    это плюс 29.2 КБ и плюс 13%, FCP вырос примерно на 0.25 c. Причина не Cormorant
    (подмножество отработало, 1.9 КБ вместо 14 КБ), а Golos Text, он в латинице
    38 КБ на вес против 24.6 КБ у Manrope. Решение «мириться или резать веса» у CDO.
  - Inter не подключать. Ниже 16 px вес не опускать ниже 400.
  - Проверять узбекскую букву `ʻ` (U+02BB), она ломается в части шрифтов.
- **ЛОВУШКА ВОРДМАРКА, стоила бы бренда.** В v4 переменная `--serif` использовалась
  ровно один раз, и это был вордмарк DEVAGO. Если при смене шрифта просто перенаправить
  `--serif` на Prata, логотип МОЛЧА меняет гарнитуру: ни ошибки, ни предупреждения,
  заметно только глазами. Поэтому заведена отдельная `--wordmark`. Не сливать обратно.
- **Шрифты подключаются ДВУМЯ отдельными `link`.** Параметр `&text=` в Google Fonts
  применяется КО ВСЕМ семействам запроса, а не к тому, рядом с которым написан. В одном
  общем запросе Prata и Golos приехали бы обрезанными до букв вордмарка, и весь русский
  с узбекским выпал бы в системный шрифт. Проверено ответом API. Сливать НЕЛЬЗЯ.
- **Рестайл REBEL** = чужая ОБОЛОЧКА (структура, ритм, форма карточек) плюс НАШ контент.
  Тексты REBEL не переносились ни строкой.

## 3.1 Технические находки (грабли, на которые уже наступили)

- **Headless Chrome на macOS игнорирует `--window-size` меньше 500 px**: верстает на
  500 и обрезает картинку. Мобильную ширину мерить только через html-обёртку с
  `<iframe width=390>` в окне не меньше 600, с флагом `--allow-file-access-from-files`.
- **zsh не разбивает строку на слова** в `set -- $var`. Из-за этого проверка «оба языка»
  четыре раза молча проверила один и тот же русский. Параметры передавать явно.
  (04.08 наступил на это повторно, уже читая эту строку. Пиши `run 390 uz`, не `for`
  по строке пар.)
- **`timeout` на macOS НЕТ**, это утилита GNU (есть `gtimeout` из coreutils, если
  поставлен). Прогон вида `timeout 90 chrome ... | sed ...` падает с кодом 127, но
  пайплайн завершается успешно, потому что код возврата берётся от `sed`. В итоге
  замер выдаёт ПУСТОТУ, и она выглядит как «переполнений не найдено». Проверять надо
  не молчание, а наличие результата: если маркера в выводе нет, писать «прогон не дал
  результата», а не считать это успехом.
- **Профиль браузера залипает после `pkill`.** Если убить headless Chrome на середине,
  в каталоге `--user-data-dir` остаётся замок, и следующий запуск с тем же профилем
  висит бесконечно без единого сообщения. Профиль на каждый прогон свой, после
  аварийной остановки каталоги профилей удалять.
- **`--virtual-time-budget` НЕ ЗАВЕРШАЕТСЯ на странице с бесконечной анимацией.** С 04.08
  у стрелки первого экрана вечный цикл, и Chrome с этим ключом ждёт «покоя» страницы,
  которого не наступает никогда: первый прогон в пачке проходит, дальше браузер висит
  минутами без сообщений. На сам сайт это не влияет, ломается только измерительный стенд.
  Лечение: сторожевой таймер вокруг каждого запуска, потому что GNU-шного `timeout` тут
  нет. Рабочая связка: `chrome ... & PID=$!; (sleep 70; kill -9 $PID) & WD=$!;
  wait $PID; kill $WD`. И обязательно отдельная проверка, что результат ВООБЩЕ появился:
  пустой вывод обязан печатать «прогон не дал результата», иначе тишина читается как
  «нарушений не найдено». За сутки этот класс ошибки поймал меня трижды.
- **Lighthouse: десктопный и мобильный профили дают РАЗНЫЕ числа, не путать.** Мобильный
  идёт с троттлингом процессора и сети и всегда ниже. Все исторические замеры по этому
  проекту мобильные (97 у v5.1, 78-80 у v2), поэтому сравнивать можно только с мобильным.
  Десктопный прогон 04.08 дал 100/100/96, и это НЕ то число, по которому принимается порог.
- **`.sol .tx span{display:block}` перебивал `.mb{display:none}`**, и на десктопе
  показывались оба варианта заголовка. Лечится дочерним селектором и `!important`.
- **Функцию смены языка надо вызывать и при загрузке**, а не только по клику: иначе
  на языке по умолчанию не подставляются десктопные и мобильные варианты строк.
- **Lighthouse 98 → 100 по доступности**: снять `aria-label` с логотипа-ссылки
  (несовпадение подписи с содержимым) и обернуть секции в `<main>`.
- **Скриншот с открытой модалкой** в headless требует запаса виртуального времени
  (не меньше 14 секунд) либо отключения CSS-переходов, иначе снимок ловит анимацию.
- **`.wikimoth/` не должен попасть в коммит** - проверять `.gitignore` перед push.
- **Vercel**: превью-деплои закрыты корпоративным входом, поэтому показывать клиенту
  можно только прод. Отсюда правило «прод трогаем по команде».

## 3.2 Что пробовали и отвергли

- **Готовые изображения из папки `dk-*`** (лампы Эдисона в hero, уличное небо в блоке
  «звёздное небо») - выброшены, не тот смысл. Текущие ассеты вырезаны из самих эталонных
  экранов и увеличены с резкостью.
- **Вписанные в макет русские подписи** на рендере «идеальная комбинация» - убраны
  построчной интерполяцией фона, чтобы можно было ставить свои подписи и переводить.
- **Название ILMAR** и любые следы чужого бренда - запрещены.
- **Одна кнопка CTA** вместо двух - отвергнуто группой.
- **Показ клиенту недоделанной версии** - запрещено: сначала CDO, потом Тахир, потом
  Икром. Скриншоты сдаются CDO, а не напрямую заказчику.

---

## 4. Незакрытое

### Ждёт данных от Икрома и решения Тахира (свап-точки в коде)
| Что | Где в коде | Сейчас |
|---|---|---|
| Ссылка на Telegram | `TG_URL` | плейсхолдер `t.me/TG_PLACEHOLDER` |
| Аккаунт Instagram | `INST_URL` | плейсхолдер `instagram.com/INST_PLACEHOLDER` (заведён v9) |
| Телефон, подпись и цель | `PHONE_DISPLAY`, `PHONE_TEL` | демонстрационный, две переменные: в `tel:` не должно быть пробелов |
| Транспорт формы заявки | `FORM_ENDPOINT` | плейсхолдер, форма честно уводит в Telegram |
| Отзывы клиентов | массив `REVIEWS` | 4 цитаты-заглушки, помечены как заглушки |
| Почта | комментарий `EMAIL` | не поставлена сознательно, до реального адреса |
| Телефон | `+998 90 123 45 67` | демонстрационный |
| Ключи аналитики | `YM_PLACEHOLDER`, `PIXEL_PLACEHOLDER` | счётчик выключен |
| Рендеры высокого разрешения | `assets/cn-*` | вырезки из макета, годятся для показа |
| Логотип в векторе | `assets/logo.png` | растр, обтравка |
| Гарнитура заголовка hero | `@font-face 'HeroH1'` | активен Forum, переключается ИМЕНЕМ ФАЙЛА в одной строке |

### Известные хвосты
- Lighthouse для v5.1 не перепроверен (см. раздел 2).
- Прод показывает старую версию: пока это осознанно, но чем дольше, тем выше риск,
  что Икром откроет ссылку и увидит не то, что ему показывали на скриншотах.
- Липкая мобильная кнопка не проверялась в бою, она выключена флагом.
- Живая проверка на реальном iPhone (не в эмуляторе) не проводилась.

---

## 5. Правила работы с этим репозиторием

- Скриншоты в `review/` не переписывать и не чистить: это доказательная база сдач.
- Отчётов в виде markdown-файлов в репозитории не заводить. Продукт - это `index.html`
  и `assets/`. Единственное исключение - этот файл заметок.
- Отчёт о работе идёт сообщением в рабочий чат, статус проекта живёт в файлах штаба.
- Деплой - только по явной команде владельца проекта.

---

## 6. Спеки CDO v3.1 и v4, ветка preview-mobile (04.08.2026)

### Что сделано
- **v3.1 «стена, вымытая светом»**: фиксированный слой `.wall` с тремя фонарями-градиентами
  (периоды 74/88/96 c, прозрачность .055/.045/.032), нефиксированный `.wall-hero` с маской
  на первый экран, секции стали полупрозрачными (rgba .94 десктоп, .96 телефон),
  дыхание кромки 24 c со сдвигом фазы. На телефоне дрейф выключен, свет и дыхание остались.
- **v4**: единое модальное окно на весь сайт (30 записей в `MODALS`, ловушка фокуса,
  возврат фокуса, блокировка прокрутки, лист снизу на телефоне), бургер скрыт от 1000px
  с переносом RU/UZ в шапку, орнамент светового разрыва в трёх местах, карточки решений
  и стандартов стали кнопками, выноски комбо-фото интерактивны (поповер от 900px,
  окно ниже), секция «Звёздное небо» заменена на «Варианты фотопечати» (8 плиток),
  отзывы перестроены (лента со scroll-snap до 600px, две колонки выше), финальная
  секция из трёх зон стала двумя, логотип уехал в футер.

### Дефекты, найденные ЗАМЕРОМ (а не по спеке) и починенные
1. `.deep` имел непрозрачную базу `#000` на z-index -1 и полностью закрыл бы стену (-2).
2. База стены из спеки (`#0a0806`/`#070605`) возвращала дефект GM: зазор против полотна
   1.023 к 1. Тона сдвинуты вниз, зазор стал pure black, контраст 1.043 -> 1.056.
3. Двухколоночная сетка комбо включалась с 900px, где фото получает 444px (на 768 без
   сетки оно 718px). На этом провале подписи 2 и 3 налезали на -3.5px. Порог поднят до 1100.
4. Правило `max-inline-size:24%` для подписей несовместимо с требованием зазора: ближайшие
   якоря 35% и 56%, между ними 21%, значит потолок ширины это 18%. Исправлено арифметикой.
5. Точки карусели раздувались до 44x44: старое `.car-ctl button` задаёт min-width/min-height
   всем кнопкам блока, а мой селектор их не перекрывал.
6. Первый пункт меню ломался на две строки и вставал вплотную к локапу лого (нет nowrap).
7. Пункты меню были 81x38 при пороге 44.
8. `alt` картинки в окне склеивал оба варианта текста (.dt и .mb лежат в одном ключе).
9. `label-content-name-mismatch`: `aria-label="DEVAGO Ceiling"` не содержал видимого
   «DEVAGOCEILING». Ярлык снят, знак сделан декоративным.
10. `target-size`: Lighthouse меряет бокс САМОГО элемента, накладка через `::after` ему
    не видна. Точки стали кнопками 44x44 с видимым кружком в `::before`.
11. Вставка блока CSS дважды промахивалась мимо основного листа стилей и попадала в
    шрифтовой `<style>` в head. В файле ДВА тега `</style>`, вставлять надо в последний.

### Открытое, решение не за вёрсткой
- **Подпись «Магнитные трековые системы» это три строки на всех ширинах** (спека требует
  до двух). Причина словесная: «МАГНИТНЫЕ ТРЕКОВЫЕ» это 18 знаков, около 117px, а потолок
  ширины по зазору 95-97px. Двумя строками эта фраза при этих якорях не встаёт никак.
  Дешёвое лечение есть: ставить на фото короткую форму «Магнитные треки» (она уже есть
  в ключе `s3_h`), а полное название оставить в поповере. Это правка клиентского текста,
  поэтому за CDO.
- **Длина строки отзыва на 768 это 41 знак при заявленных 45-75.** Спека требует
  одновременно две колонки от 600px и кегль не ниже 14px, при 354px карточки это
  несовместимо. Одна колонка в вилке 600-899 даёт 45+, решение за CDO.
- **Фото объектов в отзывах** заменены плейсхолдерами из ассетов репозитория 1600px:
  прежние обрезки канон-мокапа были 170-210px и в слот 16:10 не тянули физически.
  Нужны реальные фото от клиента.
- **Замена «Звёздного неба» на «Фотопечать» меняет состав секций**, а каркас утверждал
  Икром. Услугу он подтверждал раньше, но факт замены надо сообщить ему ДО показа.

### Lighthouse: почему появился `tools/minify.py`
Три прогона на одном железе, разброс в пределах балла:

| сборка | performance | LCP | a11y / bp / seo |
|---|---|---|---|
| старая, c516b9f | 98, 98 | 2.0-2.1 c | 100 / 100 / 100 |
| новая, исходник как есть | 87, 87 | 2.5-2.6 c | 100 / 100 / 100 |
| новая, после `tools/minify.py` | 97, 98 | 2.2-2.3 c | 100 / 100 / 100 |

Причина падения ровно одна: документ вырос со 102 674 до 167 946 байт, он
рендер-блокирующий, и 18 процентов его веса это комментарии. Комментарии остаются
в исходнике (они уже несколько раз ловили ошибки каскада), на прод уезжает копия
после `python3 tools/minify.py`. Gzip после чистки 27 595 б, то есть МЕНЬШЕ, чем
у прежней версии (28 951 б).


---

## 7. Спека CDO v5: модальное окно, десктопная раскладка (04.08.2026)

### Замеренная причина правки
При `max-width:720px` и `aspect-ratio:16/10` фото фиксировано 449px по высоте независимо
от высоты окна. Текстовой зоне на 901x607 оставалось 116px при 275px спрятанного текста,
на 1440x810 остаётся 319px. Дополнительно, чего в спеке не было: строка при этом 83 знака,
то есть ВЫШЕ вилки 50-75, а не только зона коротка.

### Что сделано
- **Разворот от 1000px**: сетка `minmax(0,1fr) 440px` (480px от 1360px), фото слева
  тянется по высоте ряда, текст справа, между ними волосяная линия через `border-left`.
- **Планшетная полоса до 999px**: фото ограничено `min(40vh,320px)`. На телефонах правило
  неактивно ПО ПОСТРОЕНИЮ: при 390x844 фото 244px против потолка 320, при 360x640 фото
  225px против 256. Геометрия листа снизу не изменилась ни на пиксель, замерено.
- **Закреплённая полоса действия** с двумя кнопками (золотая на форму, контурная в Telegram),
  прижата к низу через `margin-top:auto`, липкая при переполнении.
- **Переходы по набору**: стрелки и счётчик «3 / 8», `aria-live="polite"`, клавиши
  ArrowLeft/ArrowRight, фокус остаётся на нажатой стрелке. Набор собирается по ПОРЯДКУ
  В DOM внутри секции источника, ключи не разбираются (они непоследовательны: sol-1,
  pp1, std-1). Выноски комбо подхватываются через `data-ann`, поэтому на телефоне
  у них тоже набор из пяти.

### Дефекты, найденные замером (задача 5 и сверх неё)
Гипотезы CDO про счётчик замков и две одновременные ловушки фокуса **не воспроизвелись**:
форма открывается только ПОСЛЕ закрытия окна, поэтому одновременно открыто всегда одно.
Счётчик всё равно внедрён, чтобы поведение не зависело от порядка вызовов. Реально
найдено другое:

1. **Фокус терялся при закрытии формы.** `openForm` запоминал `activeElement`, а это
   ссылка ВНУТРИ информационного окна; после его закрытия фокус возвращался на скрытый
   элемент и уезжал на `body`. Теперь запоминается карточка, с которой окно открыто.
2. **Замок прокрутки не работал вообще.** `body` получал `overflow:hidden`, но
   прокручивается `documentElement`, и он оставался `visible`. Дефект жил с v4.
3. **Даже после этого страница уезжала на 315-510px при открытии окна.** Трассировка
   показала: ни одного вызова `scrollTo` или `scrollIntoView`, браузер сам тянул в вид
   элемент, получивший фокус, ИГНОРИРУЯ `preventScroll`. Лечится заморозкой тела
   (`position:fixed` + `top:-Y`) с точным возвратом позиции при закрытии.
4. **И после заморозки вёрстка ниже уезжала ровно на 16px.** Причина: при заморозке
   `scrollY` становится нулём, слушатель шапки снимает класс `.shrunk`, и шапка растёт
   с 74 до 90px. Слушатель теперь молчит, пока страница заперта.
5. **Крестик окна был 43.86px** по ширине, на 0.14px ниже порога. Та же ловушка, что
   с кнопкой закрытия меню в v2: глиф не даёт целого числа.
6. **Строка грида берёт высоту по содержимому**, а `max-height` на контейнере её не
   сжимает, только обрезает. На 1440 тело выходило 663px при окне 640, и полоса действия
   уезжала за нижний край. Лечится `grid-template-rows:minmax(0,1fr)`.
7. **Кадры окон решений были панорамными** (`cn-combo` 2.70, `cn-star` 3.37) и теряли
   53-66% при потолке 20%. `object-position` такое не лечит, он только выбирает, что
   уцелеет. Заведены `cn-sol1..6` с соотношением 4:3 из неиспользуемых ассетов
   репозитория 1600px, обрезка стала 0-14%.

### Отступления от спеки, с арифметикой
- **Полоса действия в ОДНУ колонку, а не в две.** При двух колонках кнопка получает 170px
  на 1280 и 190px на 1440, за вычетом полей 142 и 162px. «СВЯЗАТЬСЯ В TELEGRAM» это
  20 знаков, около 146px: на 1280 не влезает, на 1440 впритык. Узбекское
  «TELEGRAM ORQALI BOGʻLANISH» это 26 знаков, около 190px, не влезает НИ НА ОДНОЙ нашей
  ширине. Обе подписи ломались на две строки в обоих языках. В одну колонку кнопка
  получает 352 и 392px, обе подписи идут одной строкой. Вернуть две колонки это одна строка.
- **Потолок меры набора 64ch** в колоночном режиме: спека чинила высоту фото, но не меру,
  и строка оставалась 83 знака на 901 и 91 на 768. Стало 72.
- **`html{scrollbar-gutter:stable}`**: при заморозке тела полоса прокрутки пропадала,
  контент становился на 15px шире и страница перевёрстывалась.

### Приёмка (цифры)
| ширина | окно | фото | строка | обрезка | кнопки | полоса видна |
|---|---|---|---|---|---|---|
| 1440x810 | 1180x640 | 698x638 | 49 зн | 4% | 391x48 | да |
| 1280x800 | 1080x608 | 638x606 | 44 зн | 0% | 351x48 | да |
| 1024x768 | 960x504 | 518x502 | 44 зн | 2% | 351x48 | да |
| 901x607 | 720x567 | 718x243 | 72 зн | 64% | 666x48 | да |
| 768x900 | 768x741 | 768x320 | 72 зн | 56% | 728x48 | да |
| 390x844 | 390x717 | 390x244 | 44 зн | 34% | 350x48 | да |
| 360x640 | 360x589 | 360x225 | 40 зн | 34% | 320x48 | да |

Обрезка в колоночном режиме (до 1000px) высокая по построению: это цена ограничения
высоты фото из задачи 2, зато тело выросло со 116 до 322px на 901.

Сдвиг секций при открытии окна: 0px на всех проверенных ширинах. Тап-цели в окне:
нарушений нет на всех семи. Переполнения ноль на девяти ширинах в двух языках.
Lighthouse мобильный после `tools/minify.py`, семь прогонов: 82, 92, 97, 97, 97, 97, 98,
**медиана 97**, LCP 2.3 c в шести прогонах из семи, CLS 0, доступность/практики/SEO по 100.
Контрольный замер v4 на том же стенде: 97, 98, 98, LCP 2.3 c. То есть загрузка не
просела, разброс даёт TBT (у v5 от 6 до 398 мс на ОДНОЙ И ТОЙ ЖЕ сборке).

### Бюджет спеки превышен
Заявлено 2.6 КБ исходника, вышло **4802 б после чистки** (+85%). Состав перерасхода:
разметка стрелок и счётчика, вторая кнопка с инлайновым SVG Telegram, плюс шесть правок
сверх спеки из списка выше (замок прокрутки, заморозка тела, стабильный жёлоб, min-width
крестика, строка грида, потолок меры). В gzip это +1014 б, на LCP не влияет.


---

## 8. Спека CDO v6: шапка, десктоп от 1000px (04.08.2026)

### Что сделано
Переключатель языка стал сегментным контролем (общая рамка, равные сегменты, состояние
различается подложкой И весом, `role="group"`, переводимый `aria-label`, `aria-pressed`
и `lang` на кнопках). Кнопка действия получила `nowrap` и контурное оформление, пока
главная золотая живёт в первом экране. Текущий раздел подсвечивается через `aria-current`
отдельным наблюдателем. Якоря получили `scroll-margin-top`. Шапка прозрачна наверху и
плотнеет после прокрутки. Появились ссылка обхода и тег `<main id="main">`, которого
в файле не было вовсе.

### Ловушки, на которые наступил
1. **Инлайновый `style` на кнопке снят, и на его месте немедленно возник тот же дефект
   специфичностью.** Правило `.nav-right .cta` это 0,2,0, а `.btn-gold` всего 0,1,0,
   поэтому при повышении кнопка МОЛЧА оставалась контурной. Хуже: сокращённая запись
   `background` обнуляла `background-image`, то есть градиент и блик от 05.08.
   Оформление контурного состояния привязано к классу `btn-out`, который скрипт снимает.
2. **`.shrunk` вешается на 40px прокрутки, а не когда ушёл первый экран.** По спеке
   золотое повышение шло за `.shrunk`, и в окне между 40px и уходом hero на экране было
   бы ДВЕ золотые заливки, ровно то, что спека запрещает. Повышение завязано на отдельный
   наблюдатель по `.hero`.
3. **Ломается русский, а не узбекский.** Спека предполагала обратное. Строка меню в RU
   это 453px против 442px в UZ, кнопка 199.6px против 161.5px.

### Порядок уступок, как он реально отработал
`nowrap` сделал провал видимым: на 1000 RU появилось переполнение 9px, на 1200+ запас
стал -5px. Уступка 1 (трекинг `.cta` до .10em) дала 20px и закрыла все ширины от 1200.
На 1000 не хватало ещё 17px, поэтому зазоры в сжатой вилке дожаты: `.dnav` gap 14 -> 10,
`.nav-right` gap 10 -> 8, `.lang button` min-width 48 -> 44. Кегль меню НЕ резался ниже
`.64rem`, порог показа десктопного меню поднимать не потребовалось.

### Приёмка (цифры)
| ширина | язык | кнопка | строк | меню строка | запас | переполнение |
|---|---|---|---|---|---|---|
| 1000 | RU | 199.6x44 | 1 | 453 | +13 | нет |
| 1000 | UZ | 161.5x44 | 1 | 442 | +62 | нет |
| 1100 | RU | 199.6x44 | 1 | 453 | +113 | нет |
| 1100 | UZ | 161.5x44 | 1 | 442 | +162 | нет |
| 1200-1600 | RU | 229.6x44 | 1 | 591 | +14 | нет |
| 1200-1600 | UZ | 187.5x44 | 1 | 581 | +67 | нет |

До правки подпись была в ДВЕ строки на всех шести ширинах в обоих языках.

Контраст в шапке наверху страницы: пункты меню 7.88-8.00, вордмарк 15.5-15.6,
CEILING 8.04-8.11 при пороге 4.5. Активный сегмент языка 8.57 (CDO считал 8.9).
Все шесть якорей попадают, заголовок виден целиком на 1440 и на 390. Первым в порядке
табуляции фокусируется «К содержимому». Переполнений ноль на двенадцати ширинах
в двух языках. Lighthouse мобильный после `tools/minify.py`, пять прогонов:
90, 96, 96, 97, 98, **медиана 96**, LCP 2.3 c, CLS 0, доступность/практики/SEO по 100.

### Приём «прозрачная шапка над фото» прошёл НЕ так, как задумано
Шапка липкая, но при `scrollTop=0` она стоит НАД первым экраном, а не поверх него:
низ шапки 87px, верх hero 87px, перекрытия ноль. Поэтому сквозь неё видно не фотографию,
а стену с фонарями. Замер контраста сделан по тому, что реально позади, и порог взят
с запасом. Чтобы приём заработал как задуман, шапку надо положить ПОВЕРХ первого экрана
и добавить hero верхний отступ, это изменение композиции, за CDO.

### Бюджет
Заявлено 2048 б, вышло **3050 б после чистки** (+49%, против +85% у v5). Перерасход:
тег `<main>` и ссылка обхода, разведение специфичности контурного состояния, отдельный
наблюдатель за первым экраном вместо `.shrunk`, механизм `data-i18n-label` для перевода
`aria-label` группы, дожатые зазоры сжатой вилки. В gzip это +704 б.


---

## 9. Спека CDO v7: стрелки, сетка решений, оговорка, шапка, кнопка (06.08.2026)

**Задача 1.** Стрелки разведены атрибутом `data-modal-nav="off"` на секции, ключи не
разбираются. Правило записано в комментарии у `imBuild`: стрелки нужны там, где набор
это ПОСЛЕДОВАТЕЛЬНОСТЬ или ГАЛЕРЕЯ, и не нужны там, где это МЕНЮ разных предложений.
Во всех шести окнах решений на 1280 и 1440 в RU и UZ внутренней прокрутки НЕТ, поля
резать не потребовалось. Регрессия чистая: фотопечать 1/8, стандарты 1/6, отзывы 1/5,
клавиши и кнопки листают.

**Задача 2.** Шесть колонок стали тремя. Замер ДО: карточка 140px на 1000 и 187px на
1440, заголовки от ОДНОЙ до ЧЕТЫРЁХ строк в одном ряду (RU на 1440: 4,1,3,1,1,3).
Замер ПОСЛЕ: карточка 293px на 1000 и 387px на 1440, заголовки 1-2 строки, описания
1 строка, высоты карточек равны. `min-height` выставлены по замеру: `b` 2.6em,
описание 2.8em, стрелка `margin-top:auto`.
ЛОВУШКА: `@media(700-999)` не работала без `display:grid`. Базово `.sol-list` это
flex-колонка, и смена `grid-template-columns` на ней не действует ВОВСЕ, при этом
счётчик колонок в замере молча врал (читал заданное значение, а не применённое).

**Задача 3.** Замер по запросу спеки: узел отрисован Jost и для кириллицы (34 глифа),
и для подстроки DEVAGO (6 глифов), перебивать некому.
СЛИЯНИЕ, А НЕ ЗАМЕНА: пока бриф шёл, GM коммитом `fb4f5ec` уже уменьшил кегль
(.84 / .88 от 900), поставил интерлиньяж 1.6, меру 56ch, цвет `--mut` и починил нулевой
зазор до карусели. Спековые значения (.86 / 1.62 / `--t2` / `margin:0 auto var(--s6)`)
обнулили бы его верхний отступ, ради которого правка и делалась. Добавлено только
центрирование. Расхождение по цвету отдано CDO: и спека, и GM обосновывают своё
значение словом «тише», но `--t2` светлее `--mut`.

**Задача 4.** Пара 22/48 из спеки НЕ помещается в русском: переполнение 9px на 1200
и 22px на 1000. Инвариант «зазор бренд-меню не меньше двух зазоров пунктов» сохранён,
но обе величины отмасштабированы: 20/44 до 1440 и 22/48 от 1440 (отношения 2.2 и 2.18).
Разделитель в сжатой вилке живёт ВНУТРИ отступа (`left:-10px`), а не добавляет своих
полей: `padding-left` съедал ещё 20px. Трекинг кнопки .12em держится от 1200,
в сжатой вилке остаётся .10em, там не хватало ровно 2px.

**Задача 5.** Двойная рамка через `.btn::before` с `inset:4px`, на наведении внутренняя
линия зажигается плюс тёплый спад, движения нет. У золотой внутренняя линия ТЁМНАЯ:
светлая на золоте не видна вовсе. Радиус 0 применён к `.btn`, `.modal input`,
`.info-modal .m-close`, `.skip:focus` и `.lang`. Точка карусели и ручка листа НЕ тронуты.
Острых углов поверх скруглённых фото и иконок не найдено.

**Приёмка.** Переполнений ноль на двенадцати ширинах в двух языках. Тап-цели без
нарушений на пяти ширинах, включая открытое меню. Бюджет **1891 б после чистки при
заявленных 1946**, впервые уложился.

**Lighthouse, и почему цифра не годится как абсолютная.** Машина в этот заход была
сильно нагружена: TBT гулял от 19 до 4493 мс на ОДНОЙ И ТОЙ ЖЕ сборке. Контрольный
замер с равными выборками (по 7 прогонов, вперемешку, один стенд): ДО
44/67/90/93/94/96/98 медиана **93**, ПОСЛЕ 85/86/87/92/93/96/97 медиана **92**;
LCP 2.4 c против 2.2 c. Сборки статистически неразличимы, и база САМА не берёт порог 95
на этой машине. Прямой замер без троттлинга (по 5 проб, медианы): пересчёт стиля
60.7 -> 69.1 мс, раскладка 170.9 -> 189.8 мс, то есть реальная цена v7 около 27 мс,
а не 413 мс, как показывал шумный прогон Lighthouse. Абсолютный порог снимать на тихом
стенде или на боевом хосте.


---

## 10. Спека CDO v8: мобильные выноски комбо-фото (06.08.2026)

**1. Маркер стал честной кнопкой.** Размер 44x44 перенесён на сам элемент, видимый
кружок ушёл в `::before` (28px, тёмная заливка `rgba(11,10,8,.72)` с золотым кольцом,
цифра 12px вместо 8px). Прежняя накладка `::after` погашена, иначе зона удвоилась бы.
`translate(-22px,-22px)` ставит ЦЕНТР кнопки на процентный якорь: замер подтверждает
10/15, 35/16, 56/15, 79/14, 82/69 процента, ровно заданные значения.

**ЛОВУШКА, из-за которой четыре маркера из пяти рисовались полоской.** Десктопные
`.ann.dn::before` и `.ann.up::before` это ХВОСТ выноски, и они на класс специфичнее
моего `.ann::before`. Кольцо выходило 1x26 вместо 28x28. Пятый маркер имеет класс `.nn`,
под это правило не попадал и был исправен, поэтому дефект выглядел как «сломались
четыре из пяти», а не как ошибка каскада. Лечится явным глушением геометрии на той же
специфичности.

**2. Хвостик-указатель** взят на существующий `.ann i` (он был просто спрятан), новых
узлов нет. У пятого маркера хвостик снят: он стоит на окне, указывать ему не на что.

**3. Мобильные якоря** заданы отдельными свойствами внутри `@media(max-width:560px)`.
Десктопные проценты НЕ тронуты, и это проверено попиксельной сверкой: комбо-секция
на 1440 в RU и UZ, а также на 768, 900, 1000 и 1099 совпала ПИКСЕЛЬ В ПИКСЕЛЬ
с состоянием до правки.

| маркер | десктоп | мобиль | что подписывает |
|---|---|---|---|
| 1 | 10% / 5% | 10% / 15% | теневой профиль |
| 2 | 35% / 6% | 35% / 16% | световые линии |
| 3 | 56% / 5% | 56% / 15% | магнитные треки |
| 4 | 79% / 14% | без изменений | скрытый карниз |
| 5 | 82% / 69% | без изменений | парящая подсветка |

**4. Легенда как второй способ нажать.** Пункты стали кнопками с тем же ключом
`data-ann`. Замер цели: **164x44 на 390 и 149x44 на 360** против кружка 44x44, то есть
примерно втрое больше по площади. Подсветка двусторонняя: строка метит свой маркер
и наоборот, при закрытии окна снимается.

**5. Подсказка.** Разовая пульсация кольца ровно ТРИ раза (`animation-iteration-count:3`),
бесконечный цикл запрещён: в разделе 3.1 записано, чем он кончился для стенда.
Под `prefers-reduced-motion` выключается. Подпись под фото на мобильном, ключ `ann_hint`,
тексты черновые, финал за копирайтером.

**Приёмка.** Бокс всех пяти маркеров 44.0x44.0 на 390 и на 360 БЕЗ учёта
псевдоэлементов. Контраст цифры под пятым маркером над самым светлым пикселем кадра:
подложка `rgba(11,10,8,.72)` над `rgb(143,129,100)` даёт `rgb(48,43,34)`, цифра
`rgb(222,188,119)`, контраст **7.74 к 1** при пороге 4.5 (CDO оценивал ~5.4).
Переполнений ноль на двенадцати ширинах в двух языках, тап-цели без нарушений,
JS-ошибок ноль. Lighthouse после `tools/minify.py`: 77, 94, 95, 96, 98, **медиана 95**,
LCP 2.3 c, CLS 0, доступность/практики/SEO по 100.

**Бюджет превышен втрое: 2966 б после чистки при заявленных 922.** Состав: CSS около
2000 б (мобильный маркер, глушение специфичности хвоста, хвостик, якоря, кнопки легенды,
подсказка, пульсация), скрипт около 550 б (двусторонняя подсветка), разметка около 400 б
(пять кнопок легенды, абзац подсказки, два ключа). Только код блоков из самого брифа,
без единого моего дополнения, занимает около 710 б, то есть 77% бюджета, поэтому
уложиться в 922 б с пятью задачами было нельзя. В gzip прирост 640 б.


---

## 11. Спека CDO v9: полоса цифр, звонок и Instagram, позиция навигации (06.08.2026)

**Задача 1. Полоса цифр** новой секцией `#facts` между hero и решениями. Четыре плитки,
география отдельной строкой. Неразрывные пробелы и «²» символами. Числа НЕ переносятся
ни на одной из шести проверенных ширин в двух языках; на 360 кегль режется до 23.04px,
как и задумано. Анимации счётчика нет.
⚠️ В брифе стоит ссылка «Почему 10 000, а не 19 000: см. открытый вопрос ниже», но
самого вопроса в тексте НЕТ. Поставлено 10 000+, как указано в таблице. Вопрос за CDO.

**Задача 2. Звонок и Instagram в четырёх местах.** Шапка, бургер-меню, финал с футером,
липкая полоса. Свап-точки `INST_URL`, `PHONE_DISPLAY`, `PHONE_TEL`.

**ПОРОГ 1200 ИЗ БРИФА НЕ ПРОХОДИТ, и дело не в запасе строки.** `.nav` ограничен
`max-width:1200px`, а русской строке шапки с двумя иконками нужно 1333px при доступных
1120. Не помещается НИ НА КАКОЙ ширине окна, включая 1920: на широких экранах содержимое
вылезало за правый край полосы на 125px и уходило из колонки контента, а на 1200-1440
давало переполнение документа до 111px. Одна иконка вместо двух тоже не спасает
(1265 против 1120). Иконки включены с **1440**, и там же полоса шапки расширена до 1440:
содержимое получает 1360 против нужных 1333, запас 27px. Заодно `.dnav{margin-left}`
в вилке 1200-1439 опущен с 44 до 40: 44 давало 1203px против полосы в 1200.

**Задача 3. Навигация окна переехала к картинке.** На десктопе блок ложится в ТУ ЖЕ
ячейку грида, что и медиа, поэтому ширина колонки нигде не зашита числом.
ЛОВУШКА: стоит явно поставить в ячейку (1,1) ТОЛЬКО навигацию, как авторазмещение
уводит `.im-media` в соседнюю колонку, картинка с текстом меняются местами, а стрелки
повисают над текстом. Поймано замером координат (картинка оказалась в 829..1309,
стрелки в 131..829). Лечится явной постановкой всех трёх детей.
Вторая ловушка: классы `.nav-prev` и `.nav-next` принадлежат КАРУСЕЛИ отзывов, у стрелок
окна их нет, и первая версия блока не находила ничего.

**Подложка стрелок .68, а не .55 из спеки.** Замер по всем восьми кадрам набора: на
«Геометрии» самый светлый пиксель под стрелкой это `rgb(248,210,163)`, с подложкой .55
контраст выходит 4.56 при пороге 4.5, то есть запас 0.06. GM уже перегенерировал этот
набор один раз. При .68 тот же худший кадр даёт **6.91**.

**Возврат фокуса изменён по брифу:** теперь на карточку, С КОТОРОЙ окно открыто, а не на
ту, которую долистали. В v5 я выбрал обратное; CDO в v9 сформулировал требование прямо.

**Приёмка.** Переполнений ноль на ЧЕТЫРНАДЦАТИ ширинах от 360 до 1800 в двух языках.
Тап-цели без нарушений на пяти ширинах, включая открытое меню. Липкая полоса: скрыта
наверху, появляется после ухода первого экрана, прячется под открытым окном, возвращается
после Esc, скрыта от 900px. Lighthouse: 78, 94, 96, 97, 97, **медиана 96**, LCP 2.3 c,
CLS 0, доступность/практики/SEO по 100.

**Практики упали со 100 до 96 и возвращены.** Аудит «legible font sizes» проходит при
доле текста от 12px не ниже 60%, страница стояла на 58.07%. Подписи полосы (4.81% текста
при .72rem = 11.52px) и строка географии (1.56% при .74rem = 11.84px) были ровно той
разницей. Обе подняты выше 12px (.76 и .78rem), стало 65.37%.

**Бюджет превышен вдвое: 7478 б после чистки при заявленных 3686.** Разбивка:
задача 1 около 2517 б при 1638 (x1.5), задача 3 около 936 б при 614 (x1.5), задача 2
около 4025 б при 1434 (x2.8). Главная статья задачи 2 это ВОСЕМЬ инлайновых SVG:
телефон и Instagram повторяются в шапке, меню, финале и футере. Их можно свести в один
`<symbol>` с `<use>` и вернуть около 1.5 КБ, но это отдельная структурная правка,
за решением CDO. В gzip весь прирост 1571 б.


---

## 12. Дополнения к v9 (06-07.08.2026): дефекты живой сборки и перенос иконок

### Три дефекта от 07.08

**Дефект 1, стрелки в окне решений.** Не скрывались НИ на телефоне, НИ на десктопе:
`.im-nav[hidden]{display:none}` это (0,2,0), и мои блоки позиции
`.info-modal .im-nav{display:block}` и `{display:flex}` тоже (0,2,0), но объявлены ниже,
а при равном весе побеждает нижнее. Лечится поднятием веса до (0,3,0), без `!important`.
**МОЯ ОШИБКА МЕТОДА в v7:** я проверял АТРИБУТ `hidden`, а не отрисовку, поэтому набор
«решения» отчитался как исправный. Теперь сверяется computed `display`.

**Дефект 2, полоса действий перекрывала текст.** `sticky` держал её поверх прокручиваемого
тела. Лист стал колонкой из трёх этажей, прокручивается только средний. Проверено на
390x844 и 360x640 по всем трём наборам: прокрутка доходит до низа, последняя строка видна
целиком выше кнопок.

**Дефект 3 НЕ ВОСПРОИЗВЁЛСЯ.** Счётчик на телефоне отрисовывается: «1 / 8», бокс 58x12,
кегль 11.52px (`.72rem`), цвет `--t2`, position static, внутри полосы навигации. Пустым он
бывает ТОЛЬКО в окне решений, где набор пуст, а навигации там вообще не должно быть.
То есть это было следствие дефекта 1, отдельной причины нет.

### Иконки уехали с десктопа на мобильный (дополнение от 06.08)

Правило: иконки живут в шапке ТАМ, ГДЕ ЕСТЬ БУРГЕР, до 1000px. Отменяет расширение
`.nav` до 1440 и блок `.ic-btn` из v9, шапка вернулась к геометрии v7 (на 1000 кнопка
199.6x44 в одну строку, строка меню 453). Дубли «Позвонить» и «Instagram» из бургер-меню
убраны.

**ЗАМЕР ДО ВЁРСТКИ, оценка CDO уточнена.** Локап на мобильном **146px**, а не 129:
ошибка 17px. Доступная ширина 312 на 360 и 342 на 390. Группа из трёх кнопок 140.
При исходных зазорах на 360 оставалось 6px при заявленном поле 16. По порядку уступок
из брифа: зазор `.nav-right` 6 -> 4, зазор локапа 12 -> 10, зазор строки шапки 16 -> 12.
Итог: остаток **16px на 360** и **46px на 390**, кегль вордмарка не тронут.

| ширина | доступно | локап | группа трёх кнопок | остаток | переполнение |
|---|---|---|---|---|---|
| 360 | 312 | 144 | 140 | 16 | нет |
| 390 | 342 | 144 | 140 | 46 | нет |
| 430 | 382 | 144 | 140 | 86 | нет |
| 768 | 720 | 152 | 393 (с кнопкой заявки) | 163 | нет |
| 999 | 919 | 152 | 393 (с кнопкой заявки) | 362 | нет |

Все три кнопки 44x44 без учёта псевдоэлементов. От 1000 иконок нет, `.nav` снова 1200.

### Приёмка дополнений

| набор | 390 | 1440 |
|---|---|---|
| решения | навигации нет, счётчика нет | навигации нет, счётчика нет |
| фотопечать | навигация есть, счётчик «1 / 8» | навигация есть, счётчик «1 / 8» |
| стандарты | навигация есть, счётчик «1 / 6» | навигация есть, счётчик «1 / 6» |

Полоса действий не перекрывает текст ни в одном из шести сочетаний. Переполнений ноль
на четырнадцати ширинах в двух языках. Тап-цели без нарушений.

**Ловушка стенда, записываю чтобы не повторить:** проверка тап-целей на 1000 однажды
показала кнопку 297.8x44.0 как нарушение. Прямой замер дал ровно 44.000. Причина в том,
что снимок делался через 300 мс после возврата прокрутки наверх, то есть посреди
перехода `.reveal`. С ожиданием 1200 мс нарушений ноль. Мерить надо ПОСЛЕ покоя.

**Lighthouse.** Файл после дополнений стал МЕНЬШЕ на 689 б. Контрольный замер вперемешку
на одном стенде: до (efe4a22) 95/95/96/97 медиана **95.5**, после 87/92/96/96 медиана
**94**, LCP 2.4 c в обоих. Дальше машина деградировала, и расширенная выборка после
дополнений дала 81/84/87/92/92/96/96 медиана **92** при TBT от 91 до 588 мс. LCP при этом
не сдвинулся ни разу. Порог 95 на этом стенде сейчас не берёт и САМА база в поздних
прогонах; снимать надо на тихой машине или на боевом хосте.

⚠️ **Коммит `3e7984c` (GM, пять картинок карусели) забрал в себя мои несохранённые правки
обоих дополнений.** Код цел и запушен, но в сообщении коммита его нет. Историю не
переписывал. На будущее: перед коммитом стоит смотреть `git status`, в этом файле сейчас
работают два процесса.


---

## 13. Дополнения от 07.08: рамка золотых кнопок, гарнитура h1, полоса цифр

### Задача A. Двойная рамка только для контурных кнопок
Правило записано комментарием: контурная кнопка это ЛИНИЯ, и вторая линия делает её
богаче; залитая это ПЛОСКОСТЬ, и линия внутри плоскости читается как обводка по ошибке.
Золотые теряют рамку везде. Кнопка в шапке теряет её при повышении автоматически, потому
что повышение переключает класс `btn-gold`.

### Задача B. Гарнитура h1 первого экрана
Три кандидата собраны, все Google Fonts, вес 400, подмножество по ПРОПИСНЫМ.

| вариант | файл | LCP (медиана 5 проб, троттлинг 4x) | контраст золотой строки 390 / 1440 |
|---|---|---|---|
| Jost, как было | 0 (уже загружен) | 1324 мс | 8.58 / 10.33 |
| **Forum (активен)** | **2520 б** | 1496 мс | 8.58 / 10.40 |
| Prata | 1984 б | 2060 мс | 8.58 / 10.20 |
| Oranienbaum | 2104 б | 1564 мс | 8.58 / 10.35 |

**ЛОВУШКА ПОДМНОЖЕСТВА ЗАКРЫТА, и проверена наблюдением.** Заголовок набирается через
`text-transform:uppercase`, в разметке лежат строчные. Набор глифов выведен ИЗ САМИХ
ключей `h_h1` скриптом, а не переписан руками: 22 глифа, латиница AFGHILNRST и кириллица
АЕЖИКЛНОПТЫЯ. Наличие каждого проверено по таблице `cmap` внутри скачанного файла.
Отрисовка проверена через `CSS.getPlatformFontsForNode`: в каждом варианте отрисован
ровно один шрифт (15 глифов в RU, 14 в UZ), Jost рядом НЕ появляется, то есть двух
гарнитур в одном слове нет.

**Главный риск спеки не реализовался:** LCP-элемент во всех четырёх вариантах это
`<img>` первого экрана, а не заголовок. Подтверждено и прямым замером, и Lighthouse.

Метрики: трекинг .08em на телефоне и .06em на десктопе, интерлиньяж 1.04. В брифе как
исходные указаны .16em и .13em, но в файле на тот момент стояли **.19em и .16em**
(значения из v3), это учтено.

### Полоса цифр: мобильная раскладка и разделители
Спецификация в одну колонку до 600px, крест из линий на планшете, единица отдельным
весом, история уехала в сноску.

**ДВЕ ПРАВКИ СВЕРХ СПЕКИ, обе обязательные.** Правило `.fu` из брифа не содержит
`display`, а базовое `.facts .f span{display:block}` ловит и ВЛОЖЕННЫЙ `.fu` внутри `b`,
поэтому единица уезжала на свою строку под числом на всех ширинах от 600. И отступа
в спеке тоже нет: между числом и единицей в разметке пробела нет намеренно, поэтому
без `margin-left` выходило «16лет». На телефоне зазор даёт `flex gap`, выше нужен свой.

| ширина | самая широкая строка | остаток | строк подписей | перенос числа |
|---|---|---|---|---|
| 360 | 296px | 16 | 1,2,1,1 | нет |
| 390 | 326px | 16 | 1,2,1,1 | нет |
| 430 | 354px | 28 | 1,1,1,1 | нет |
| 768 | 654px | 66 | 1,1,1,1 | нет |
| 1000 | 364px | 556 | 1,2,1,1 | нет |
| 1440 | 504px | 696 | 1,1,1,1 | нет |

Оценка CDO уточнена: самая широкая строка это НЕ «600 000 м²», а «10 000+» с подписью
«выполненных объектов в Узбекистане», 296px против оценки 303px. Остаток на 360
получился 16px, а не 9px. Ступенька кегля `@media(max-width:380px)` осталась страховкой,
но фактически не понадобилась. Узбекская `ʻ` отрисована во всех шести ширинах.

**Приёмка.** Переполнений ноль. Lighthouse: 83, 94, 95, 95, 96, **медиана 95**, LCP 2.4 c,
CLS 0, доступность/практики/SEO по 100. LCP-элемент это `<img>`.
Бюджет: 1761 б после чистки при сумме заявленных 1229, перебор 532 б, из них обязательные
правки `.fu` около 120 б. Шрифты 2520 б в бюджет исходника не входят, на прод уезжает
ОДИН файл.


---

## 14. Спека CDO v10: самопрячущаяся шапка на телефоне (07.08.2026)

До 760px шапка стала липкой и самопрячущейся. Фон СПЛОШНОЙ `var(--bg)`, никакого
`backdrop-filter` и никакой полупрозрачности: правило про браузер Telegram в силе целиком,
менялось ПОЛОЖЕНИЕ, не материал. Локап не тронут ни на пиксель.

**Высота: 65px, а не ~52 из брифа. И до правки было 77px, а не 64.** Пол задают
ТАП-ЦЕЛИ: локап-ссылка и бургер это ровно 44px по правилу из v2, плюс поля 2x10 и
волосинка, итого 65. Чтобы получить 52, поля пришлось бы срезать до 3-4px. Шапка всё
равно стала компактнее на 12px. Отсюда `scroll-margin-top:76px` (65 плюс 11 воздуха),
а не 68 из брифа.

**Слушатель не заводился второй раз:** расширен существующий, у которого уже стоит
защита от заморозки тела. Проверяются ОБА флага, и на `documentElement`, и на `body`.
Принудительный `hide` при открытии окна повешен на счётчик замков `lockScroll`, то есть
на единственную точку, через которую проходят все окна.

**Приёмка.** Все 18 проверок якорей (6 пунктов x 3 ширины) проходят, заголовки видны
целиком. Шапка при открытом окне остаётся спрятанной и не реагирует на колесо.
Переполнений ноль на четырнадцати ширинах, тап-цели без нарушений, JS-ошибок ноль.

**Противоречие внутри брифа, решение за CDO.** Бриф требует «обе полосы одновременно
допустимы только до порога 140px», но его же скрипт возвращает шапку при ЛЮБОЙ прокрутке
вверх. Замер: на `scrollY` 1940, 1740 и 1340 после движения вверх видны и шапка, и нижняя
полоса. Оставлено как в скрипте, потому что там же сказано «постоянной остаётся только
она» (нижняя полоса), то есть прятать её при возврате шапки означало бы убирать главный
элемент конверсии. Лечится одной строкой, если CDO решит иначе.

**⚠️ РЕШАЮЩАЯ ПРОВЕРКА НЕ СДЕЛАНА.** Бриф прямо говорит: судьбу правки решает поведение
на ЖИВОМ телефоне в браузере Telegram, встроенном браузере Instagram и Safari при быстрой
прокрутке, когда складывается адресная строка, и «эмулятор тут не доказательство».
Живого телефона у меня нет. Всё измеренное выше это эмулятор. Пока проверка не сделана,
правку нельзя считать принятой, и в разделе 3 остаётся в силе прежняя запись про липкую
шапку в Telegram.

**Бюджет.** 946 б после чистки при заявленных 819, перебор 127 б (+16%). Lighthouse:
контрольный замер вперемешку на одном стенде, до v10 72/88/89/94 медиана **88.5**,
после v10 84/92/94/94 медиана **93**, LCP 2.5 против 2.4 c. То есть v10 не просадил
ничего, но абсолютный порог 95 на этой машине сейчас не берёт и база.


---

## 15. Спеки CDO v11 и v12 (07.08.2026): лёгкий поповер и удаление навигации окон

### v11. Поповер выносок работает на всех ширинах, окно из сценария ушло

**ЛОГИКА ПОЗИЦИОНИРОВАНИЯ ПЕРЕПИСАНА, и это не украшение.** Замер: на телефоне фото
около 215px, а карточка 190-200px. Прежний порядок (не влезло вниз -> переворачиваем
вверх -> не влезло вверх -> прижимаем к `top:8`) прижимал её к верхнему краю, и она
накрывала СВОИ ЖЕ точки, все пять из пяти. Новый приоритет: карточка НИКОГДА не ложится
на маркер, а если места нет ни снизу, ни сверху, она свисает ниже фото (`.combo` не режет
overflow). Проверено на 360, 390 и 430: ни одна из пяти не накрывает свою точку.

**Альфа .82, а не .72 из спеки** — это предписанный самой спекой порядок («поднимай
альфу, а не меняй цвет текста»). Замер по всем пяти маркерам: самый светлый пиксель под
карточкой это `rgb(222,188,119)`, и это НЕ фото, а золото легенды под ним, потому что
карточка свисает ниже фото. При .72 основной текст даёт ровно **4.50**, то есть стоит НА
пороге. При .82 худший случай **5.89**, заголовок 7.82.

Фокус на телефоне НЕ переводится внутрь карточки (в v5 замером доказано, что браузер
тянет сфокусированный элемент в вид, игнорируя `preventScroll`). Связь объявлена через
`aria-describedby` и `aria-expanded` на маркере. Замер: `scrollY` до и после открытия
совпадает на всех проверенных ширинах и всех пяти маркерах.

Поведение: Esc закрывает, клик вне закрывает, повторное нажатие закрывает, строка легенды
открывает тот же поповер и метит точку. Крестик 44x44. «Подробнее» открывает прежнее окно,
поэтому картинка из `MODALS` остаётся доступной.

### v12. Навигация окон удалена целиком

Удалены разметка, весь CSS (`.im-nav`, `.im-arrow`, `.im-count`, десктопная накладка и
мобильная полоса), сборщик набора, обработчики стрелок, `data-modal-nav="off"` и `aria-live`.

**ОШИБКА, КОТОРУЮ ПОЙМАЛА ПРИЁМКА.** Крестик, клик по подложке и ловушка фокуса лежали
ВНУТРИ того же куска кода, что и обработчики стрелок, и удаление снесло их заодно.
Esc продолжал работать (он висит на документе), поэтому потеря не бросалась в глаза:
окно закрывалось клавишей, но НЕ крестиком и НЕ подложкой. Столбец «крестик» в
шестиклеточной таблице показал `false` во всех шести клетках. Восстановлено.

**Шестиклеточная приёмка (решения, фотопечать, стандарты x 390 и 1440):** стрелок нет,
счётчика нет, ArrowLeft и ArrowRight ничего не делают, Esc и крестик работают, после
закрытия страница стоит ровно на исходной карточке и фокус на ней. Ловушка фокуса:
12 нажатий Tab из окна не выпускают.

### Общее

Переполнений ноль на четырнадцати ширинах в двух языках, тап-цели без нарушений,
JS-ошибок ноль. **Байты: v11 и v12 вместе дают −3110 б после чистки** (−685 б в gzip).
v11 добавил около 900 б, v12 вернул около 4000 б, то есть больше обещанных 0.6-0.9 КБ,
потому что вместе со стрелками ушли десктопная накладка и мобильная полоса из v9.

**CLS 0.01 не связан ни с v11, ни с v12, ни с виджетом чата.** Контрольный замер:
сборка v10 (`c7b75d7`, до виджета и до этих правок) даёт ровно тот же 0.01 в трёх
прогонах из трёх. Lighthouse на успокоившейся машине: **95, 95, 95**, LCP 2.4-2.6 c,
доступность/практики/SEO по 100.

⚠️ Коммиты `dd517c2` и `1cb1053` (GM) снова забрали мои несохранённые правки: v11 попал
в коммит про виджет чата, v12 в следующий. Второй хотя бы назван «правки craftsman».
Код цел. Это уже второй такой случай, см. раздел 12.


---

## 16. Спека CDO 07.08, задачи B и C (задача A сдана отдельно)

**Задача A (облик виджета) в этом документе повторяет бриф, сданный коммитом `36c2426`**
в репозитории штаба (`ai-admin/widget/widget.js`). Сверено по пунктам A1-A6, расхождений
нет. Две ловушки, найденные там замером, записаны в recap: невалидная сокращённая запись
`font` с `inherit` (поле ввода получало 13.33px вместо 16px, из-за чего Safari на iPhone
зумил страницу при фокусе) и `rem` внутри теневого дерева, который считается от корня
чужого документа. По апострофу `ʻ` механизм оказался не тем, что в брифе: `@font-face`
документа-хозяина ДОСТАЁТ до Shadow DOM, ломался стек клиента без донора.

### B. Ритм заголовочного блока

Правило: внутри связки «кикер, заголовок, подзаголовок» зазоры нарастают сверху вниз,
а зазор ОТ предыдущего блока не меньше ТРЁХ внутренних.

| связка | до | после | правило |
|---|---|---|---|
| окно услуги 390: картинка -> кикер | 20 | **32** | 32 к 6, это 5.3 к 1 |
| окно услуги: кикер -> заголовок | 8 | **6** | |
| окно услуги: заголовок -> текст | 16 | **10** | |
| окно услуги 1440: padding-top | 44 | 44 | 44 к 6, это 7.3 к 1 |

**Остальные связки правило УЖЕ держат, проверено:** первый экран (кикер это первый
элемент блока, предыдущего блока нет; внутренние 10 и 16 нарастают вниз), фотопечать
(шапка -> заголовок 48 при внутреннем 16, ровно 3 к 1 на 390 и 4 к 1 на 1440), карточка
проекта (фото -> объект 24 при внутреннем 3, это 8 к 1). Выравнивать больше нечего.

### C. Легенда выносок в одну колонку

| замер | до | после |
|---|---|---|
| раскладка | сетка 2 колонки | одна колонка |
| строк на название | 2-3 | **по одной у всех пяти** |
| бокс строки | около 160x30 | **312x44 / 342x44 / 382x44** |
| цифра | голая, 9px | **кружок 22x22 с кольцом** |
| высота секции на 390 | 762 | **844** |

**Прирост секции +82px**, а не ~130 из оценки спеки: строк пять по 44 против прежних
трёх по 30, но исчезли внешние поля сетки. Высота страницы выросла на те же 82px.

**ЛОВУШКА: сетку нельзя задавать И на `li`, И на кнопке.** При двойной сетке кнопка
попадает в первую колонку шириной 22px, сжимается до неё, и названия едут в 2-3 строки.
Поймано замером бокса: 22x44 вместо 342x44, при том что визуально это выглядело просто
как «длинные названия переносятся». Сетка оставлена ТОЛЬКО на `.lg-btn`.

### Приёмка

Переполнений ноль на четырнадцати ширинах в двух языках. Все пять строк легенды не ниже
44px без учёта псевдоэлементов на 360, 390 и 430 в обоих языках. Подсветка двусторонняя:
кружок в легенде и маркер на фото загораются вместе. Байты B и C: **+830 б** после чистки.
Lighthouse, девять прогонов: 67, 78, 79, 89, 92, 94, 95, 96, 96, **медиана 92**,
LCP 2.5 c, CLS 0.01, доступность/практики/SEO по 100. TBT гулял от 7 до 698 мс на одной
и той же сборке, поэтому абсолютный порог 95 на этом стенде снять по-прежнему нельзя;
на прогонах с чистым TBT (7-52 мс) выходит 92-96.


---

## 17. Спека CDO 07.08: интерлиньяж, подпись проектов, одно слово в узбекском

### A. Интерлиньяж мелкого текста

Замер подтвердил диагноз: `.pp .pp-d` задаёт кегль `.74rem` и НЕ задаёт `line-height`,
наследуя от `body` 1.72, то есть **20px просвета на 11.84px букве**, при том что заголовок
той же карточки плотнее (`.66rem/1.4`). Описание было разрежено сильнее своего заголовка.
После правки расстояние между базовыми линиями **20px -> 17px**, ровно как предсказывала спека.

**Найдено ПРОГОНОМ ПО ЖИВОМУ DOM сверх списка спеки** (искал текстовые узлы мельче
`.8rem` с унаследованным отношением выше 1.6) и добавлено в первый селектор:
`.kick` (10.88px, на телефоне шапки секций переносятся в две строки), `.prj .role`,
`.foot-tx .small`, `.ann-legend .lg-btn`.

**ПОПРАВКА К СПЕКЕ:** селектора `.final-r .tag` НЕ СУЩЕСТВУЕТ, блок `.final-r` убран
в v4, когда логотип уехал в футер. Заменён на `.foot-tx .tag`.

**Не тронуто осознанно:** `.hero .sub2` имеет СВОЙ `line-height:1.7` при кегле 11.2px,
то есть формально под критерий «без своего line-height» не попадает, но принцип спеки
нарушает. Это набор капсом с трекингом в первом экране, менять его без отдельного
решения не стал. Вопрос к CDO.

### B. Подпись под заголовком проектов

| замер | до | после | цель спеки |
|---|---|---|---|
| десктоп: заголовок -> подпись | 64 | **24** | 24 |
| десктоп: подпись -> карточки | 32 | **64** | 64 |
| десктоп: отношение | 0.5 | **2.7** | не хуже 2.5 |
| телефон: заголовок -> подпись | 48 | **16** | 16 |
| телефон: подпись -> карточки | 24 | **48** | 48 |
| телефон: отношение | 0.5 | **3.0** | не хуже 2.5 |

Мелкая поправка к спеке: до правки на десктопе было **64px**, а не 76. Спека сложила
`margin-bottom:var(--s8)` шапки (64) и `margin-top:var(--s3)` подписи (12), но это
СОСЕДНИЕ элементы, их вертикальные поля схлопываются в максимум, а не складываются.

### C. Одно слово в узбекском словаре

`sexlarimiz` -> `ishlab chiqarishimiz`, апостроф U+02BB скопирован, не набран.
Русский ключ не тронут.

### ⚠️ index.min.html был УСТАРЕВШИМ, и это важнее самой задачи C

На прод уезжает `index.min.html`, и он лежит в репозитории с коммита `27007b2`.
Проверено: файл держал состояние ДО спек v11, v12 и до задач B и C предыдущего брифа,
то есть **прод отставал от исходника на три сдачи**. Пересобран через `tools/minify.py`:
139 918 -> 141 220 б. `grep -c sexlarimiz index.min.html` даёт **0**,
`grep -c "ishlab chiqarishimiz"` даёт **1**.

Побочное подтверждение: CLS в отчётах Lighthouse был **0.01** на устаревшем артефакте
и стал **0** на пересобранном. То есть тот CLS был следом старой сборки, а не дефектом.

**ПРАВИЛО НА БУДУЩЕЕ: после любой правки `index.html` обязателен
`python3 tools/minify.py`, иначе изменение до клиента не доедет.**

### Приёмка

Переполнений ноль на четырнадцати ширинах в двух языках. Lighthouse по ПЕРЕСОБРАННОМУ
артефакту, девять прогонов: 56, 58, 69, 81, 91, 94, 94, 94, 96, **медиана 91**;
на шести прогонах с TBT не выше 200 мс медиана **94**; LCP 2.4-2.8 c на чистых прогонах,
CLS **0**, доступность/практики/SEO по 100. Порог 95 на этом стенде снять по-прежнему
нельзя, TBT гулял от 21 до 1014 мс на одной и той же сборке.


---

## 18. Серый текст в карточках стандартов (07.08.2026, задача D)

**Причина НЕ в отступах, и поэтому прежние правки ритма её не брали.** У `.step .tx b`
стоит `min-height:2.8em`, то есть заголовок всегда держит высоту ДВУХ строк.
«Профессиональный монтаж» занимает одну, вторая остаётся пустой и толкает серый текст вниз.

Замер на 390 до правки: заголовок 33px при одной строке 16.6px, то есть **16px пустоты**
плюс `margin-top:7px`, около 23px сверху против **14px** до фотографии снизу. Текст был
ближе к фото, чем к заголовку, который он поясняет.

| ширина | колонок | min-height | заголовок | заголовок -> текст | текст -> фото | низы в рядах |
|---|---|---|---|---|---|---|
| 360 / 390 / 430 | 1 | **0** | 17px, 1 строка | **6** | **24** | выровнены во всех 6 |
| 600 | 2 | 33.15px | 33px, 2 строки | 7 | 14 | выровнены во всех 3 |
| 900 | 3 | 33.15px | 33px, 2 строки | 7 | 14 | выровнены во всех 2 |

Порог 599 по сетке: с 600px карточки встают в две колонки и `min-height` снова нужен.
В карточках решений та же ловушка вылечена тем же способом ещё в v7, до стандартов
правка не дошла.

**Ловушка стенда:** проверка «низы выровнены» через один общий `Set` бессмысленна, она
всегда даст столько значений, сколько рядов. Сравнивать надо ПОРЯДНО, группируя карточки
по координате верха.

**Приёмка.** Переполнений ноль на четырнадцати ширинах в двух языках. Lighthouse по
пересобранному `index.min.html`: 91, 96, 96, 96, 97, **медиана 96**, четыре прогона из
пяти сами по себе не ниже 95, LCP 2.4 c, CLS 0.004, доступность/практики/SEO по 100.
Впервые за несколько заходов стенд был достаточно тихим, чтобы порог взять.

---

## 19. Четыре правки Тахира по живой сборке (07.08.2026)

Заказ пришёл не брифом, а тремя снимками экрана из встроенного браузера Telegram.
Четыре пункта: стрелка первого экрана не нажимается, шапка мерцает, круг виджета
наезжает на липкую полосу, переход по разделу из меню мгновенный.

### 19.1 Стрелка первого экрана стала ссылкой

Была `<div class="hero-arrow" aria-hidden="true">`, то есть украшение: ни ссылки, ни
роли, ни тап-цели. Внутри обёртки появилась ссылка `.hero-arrow-b` на `#facts`,
**48x44** по правилу тап-целей. Обёртка оставлена намеренно: без неё ссылкой стала бы
вся колонка шириной 342px и ловила бы промахи по фону. Подпись через `data-i18n-label`,
ключ `arrow_lbl` заведён в обоих словарях (`Keyingi boʻlimga`). Стрелка живёт только
до 900px, выше `.hero-arrow{display:none}` из v2.

### 19.2 Шапка мерцала: сравнение с соседним кадром вместо хода от разворота

**Дефект воспроизведён на стенде и измерен.** Старое правило сравнивало положение с
ПРЕДЫДУЩИМ СОБЫТИЕМ при гистерезисе 6px, поэтому любое дрожание больше 6px переключало
шапку на каждом событии. 24 колебания подряд:

| амплитуда | было переключений | стало |
|---|---|---|
| ±8px | 21 | **0** |
| ±14px | 24 | **0** |
| ±30px | 24 | **0** |
| ±60px | 24 | **4** |
| ровная прокрутка вниз 0→2000 | 1 | **1** |

При переходе 0.28s двадцать переключений это и есть «мерцает очень быстро». Источник
дрожания реальный: во встроенном браузере строка адреса схлопывается в пилюлю и
разворачивается, высота окна прыгает, сверху ложится инерционная прокрутка iOS.

Три заслона, каждый под свой источник:
1. **Якорь вместо соседнего кадра.** Ход считается от точки РАЗВОРОТА. Пока в одну
   сторону не пройдено 48px, шапка не трогается: колебание любой частоты в пределах
   48px не даёт ни одного переключения.
2. **Тихое окно 400ms после resize `visualViewport`.** Схлопывание строки адреса это
   скачок на десятки пикселей, порогом его не удержать, но он ВСЕГДА приходит вместе
   с изменением размера окна. Замер: 6 скачков по 90px сразу после resize дают **0**
   переключений.
3. **Пауза 300ms между сменами состояния.** Потолок на частоту. Ровно она и оставляет
   4 переключения на ±60px вместо 24.

Чтение перенесено в `requestAnimationFrame`: на кадр один расчёт вместо одного на событие.

**Ловушка стенда, стоила первого неверного вывода.** Первый замер дрожания дал 2
переключения вместо 24 и выглядел как «дефекта нет». Причина: имитация звала
`window.scrollTo(0,y)`, а на `html` стоит `scroll-behavior:smooth`, поэтому каждый шаг
имитации сам становился плавной анимацией и до цели не доезжал. В имитации прокрутки
на этой странице **только** `behavior:"instant"`.

### 19.3 Круг виджета наезжал на липкую полосу: разные системы координат

Файл `ai-admin/widget/widget.js`, не сайт. Отступ круга снизу стоял голыми `79px`, а
высота липкой полосы сайта это `65px` **плюс** `env(safe-area-inset-bottom)`. На стенде
инсет равен нулю, полоса выходит 65px, зазор 14px, дефекта не видно. Замер с
подставленным инсетом 34px (iPhone):

| инсет | высота полосы | низ круга от низа экрана | зазор было | зазор стало |
|---|---|---|---|---|
| 0px | 65 | 79 | 14 | 14 |
| 34px | 99 | 79 → 113 | **−20 (наезд)** | **14** |

Правка одна: отступы обёртки и панели считаются как `calc(Npx + env(safe-area-inset-bottom))`.
Заданные 14px зазора теперь держатся на любом устройстве, на десктопе инсет нулевой и
ничего не меняется. **Это ровно то, что видно на снимке Тахира**, и объясняет, почему
приёмка круга показывала «зазор 14px, перекрытия ноль»: она мерила на стенде с инсетом 0.

### 19.4 Переход по разделу считается сами, а не движком

На странице стоит `html{scroll-behavior:smooth}`, и на стенде переход из меню плавный:
**49 разных положений за 51 кадр**. У клиента он мгновенный, то есть на это свойство
полагаться нельзя: либо движок встроенного браузера не даёт плавную прокрутку по якорю,
либо у человека включено системное «уменьшение движения». Первое обходим, второе уважаем
(`prefers-reduced-motion` даёт мгновенный переход).

Переход считается своим циклом `requestAnimationFrame` по кубической кривой, время от
расстояния в пределах 380-900ms. Это не зависит от движка и даёт то, чего у родного
перехода нет: **шапка на время переезда удерживается на месте** (`window.__headerHold`),
а посадка считается от её ЖИВОЙ высоты, а не от заранее вбитого `scroll-margin-top`.
Замер: 47 разных положений за 86 кадров, шапка ни разу не спряталась, зазор под шапкой
**12px** на 390, 900, 1024 и 1440.

**Исключения в делегированном обработчике, без них правка молча сломала бы три сценария:**
`.form-open` и `.cta` открывают окно формы своим обработчиком, `.skip` это ссылка обхода
для клавиатуры и ей нужен родной перенос ФОКУСА, а не только прокрутка, телефон и
Instagram на старте висят на `href="#"`. Фокус на цель перехода намеренно НЕ ставится:
по опыту v5 браузер тянет в вид элемент, получивший фокус, игнорируя `preventScroll`.
Адрес поправляется через `history.replaceState`.

### Приёмка

Переполнений ноль на девяти ширинах, JS-ошибок ноль, форма и заморозка тела не задеты
(открытие, `no-scroll`, Esc, разморозка). Шапка липкая на ВСЕХ ширинах: до 760px по
v10, выше по базовому правилу, поэтому `headH()` вычитает её всегда.

**Остаточный риск, который стендом не берётся.** `env(safe-area-inset-bottom)` в
headless-браузере всегда 0, инсет подставлялся переменной. Что рисуется поверх нашего
окна во встроенном браузере Telegram, страница тоже не знает. Самая дешёвая проверка:
Тахир открывает выложенную страницу на своём iPhone в Telegram и смотрит на зазор под
кругом и на шапку при прокрутке.

---

## 20. Откат липкой шапки и орнамент в углах карточек (07.08.2026, вечер)

### 20.1 Мерцание пережило починку. Модель была неверной

Хронология важна, потому что она и есть доказательство.

1. Тахир пожаловался на мерцание шапки во встроенном браузере Telegram.
2. Я воспроизвёл дрожание на стенде, нашёл причину в скрипте (сравнение с предыдущим
   событием, гистерезис 6px) и починил: порог 48px от точки разворота, заморозка на
   400ms после resize `visualViewport`, пауза 300ms. Замеры в разделе 19.
3. Правка уехала на прод. Проверено побайтно: прод отдавал 144 397 б, `quietTill` 3
   вхождения, `hero-arrow-b` 2, старого `lastY+6` ноль. То есть у Тахира на экране была
   именно починенная сборка.
4. **Мерцание осталось.** Значит переключение классов не было причиной.

Что остаётся, если дело не в скрипте. Пара «`position:sticky` плюс `transform`»: липкое
смещение iOS считает на потоке прокрутки, трансформацию на главном, во время инерционной
прокрутки они расходятся. Сюда же `padding-top:env(safe-area-inset-top)`: встроенный
браузер схлопывает обвязку на ходу, инсет меняется, высота липкой шапки прыгает. Ни то,
ни другое стендом не воспроизводится: headless не знает ни безопасной зоны, ни отдельного
потока прокрутки.

**Решение: вместо третьей догадки взять проверенное полем.** До v10 шапка на телефоне была
статической, и жалоб не было; они появились ровно после v10. Липкость и самопрятание на
телефоне сняты, `scroll-margin-top` вернулся с 76 на 16. Замер: на 390 шапка `static`,
`transform:none`, и после прокрутки на 2000px `transform` по-прежнему `none`.

**Цена названа вслух:** замысел v10 на телефоне утрачен, меню снова достаётся прокруткой
к началу. Если прятание нужно вернуть, следующая попытка это `position:fixed` плюс
распорка в высоту шапки: липкой механики нет вовсе, конфликт с `transform` исчезает.

**Правило на будущее.** Правку, которая чинится ТОЛЬКО на устройстве клиента, нельзя
закрывать замером на стенде. Стенд подтверждает, что правка сделана, а не что дефект ушёл.

### 20.2 Орнамент: спека вешала угол не туда

Спека предлагала достроить вторую диагональ на внутреннем узле карточки, раз `::before`
и `::after` самой карточки заняты. **Замер до вёрстки показал, что угол сел бы не в угол.**
Отступы от края карточки до внутреннего узла:

| карточка | узел | 1440 | 390 |
|---|---|---|---|
| `.sol` | `.tx` | сверху 91, справа 123 | сверху 25, справа 57 |
| `.step` | `.tx` | сверху 1, справа 1 | сверху 1, справа 1 |
| `.pp` | `.bx` | сверху 218, справа 1 | сверху 123, справа 1 |
| `.final-act` | `.final-btns` | сверху 49, справа 33 | сверху 33, справа 25 |

То есть у фотопечати «верхний правый» угол оказался бы на 218px ниже верха карточки, а у
решений в 123px от правого края, прямо у текста. Ровно поэтому спека и запрещала ставить
скобки в решениях на телефоне: она чинила симптом собственного способа.

Сделано иначе: **все четыре угла рисует ОДИН псевдоэлемент** размером с карточку, восемь
фоновых полосок, по две на угол. `::after` освободился совсем. Прозрачность поднята с .18
до .28, наведение по-прежнему даёт .5, у финального блока скобка 16px через
`background-size` (прежний `width:16px` теперь схлопнул бы весь слой, это отдельная ловушка).

**Ловушка из спеки подтверждена замером:** у `.step` стоит `overflow:hidden`, и он
действует на ОБЕИХ ширинах. Со смещением `-1px` слой обрезался бы. У стандартов, и только
у них, смещение нулевое.

**Проверка видимости не глазом.** Кадры углов при увеличении 6x читались неоднозначно:
на светлой фотографии в карточке фотопечати скобка казалась отсутствующей. Сравнение двух
кадров, с орнаментом и с погашенным орнаментом, дало ответ числом: максимальное изменение
канала в углу составило 43 (решения и фотопечать), 50 (стандарты), 65 (финал) на всех
четырёх углах обеих ширин. Ноль означал бы, что скобки нет. **Скобка есть везде, включая
углы поверх фотографии, но над светлой фотографией она низкоконтрастна** — это свойство
приёма (золото .28 поверх яркой картинки), а не дефект правки: до правки верхний левый
угол фотопечати лежал на той же фотографии.

### 20.3 Черта под номером на телефоне

`.no` это блок во всю текстовую зону (292px на 390, 262 на 360, 332 на 430), поэтому
`width:auto` плюс `right:0` дают полную длину без подпорок. `opacity` правила поднята до 1,
потому что прозрачность переехала внутрь градиента: иначе прежние .35 умножились бы на
альфы и линия ушла бы почти в ничто. Зазор до заголовка 10px, налезания нет, RU и UZ
одинаково.

### 20.4 Приёмка

Переполнений ноль на четырнадцати ширинах в двух языках, JS-ошибок ноль.
**Тап-цели: на ширинах от 600 остаются кнопки легенды выносок высотой около 23.6px.
Это НЕ регрессия этой правки**, 44px им заданы только до 560px; на 390, где Lighthouse и
меряет, нарушений ноль.
